node_modules
.vercel
dist
.playwright-mcp
*.jpeg

# visual-regression captures (large PNGs, regenerated on demand)
vr/
vr2/

# probe build output (production bundle + measurement hooks)
dist-probe/

# texture candidates for the board picker (dev only until one is chosen)
public/_textures/
.env*
