# THE BASELINE — 2026-08-08

*Declared by Tirtha after live play: "this is our baseline and this is
absolutely perfect right now… if anything regresses, we know what's
going wrong." Git tag: `baseline-2026-08-08` (commit `3a38ce8`), live on
tirthajyotighosh.com the same day.*

## What the baseline protects (the sensory contract)

- **The spray**: shutter-streak tracers — dense golden ribbons when
  turrets converge, comet dashes fanning under saturation, HDR bloom on
  every round. "Extremely show specific… I like it way too much."
- **The duel**: torpedoes visibly dodge the streams (near-miss jukes) —
  "a battle between the torpedoes and the PDC rounds."
- **THE SCOPE**: edge-lit vessel, no drawn plane, tinted surface,
  ring at 0.48 of the radius ("very well defined… up to my mark"),
  collapse morph, 132→260 px combat growth, TEWA tints, corridor read,
  T-digit.
- **THE VOICE**: four-tier arbitration, flavor off-center, coach at 84%
  viewport in plain language, alarms ~5 s (~10 finite blinks), no ghost
  blink, countdown hold-band (no 10S pin).
- **The ladder**: W1 teaches (parked: 0 hits), W2 bleeds, W3 kills the
  parked (measured 4/7 + damage 7/7; sustained orbit survives at 33%).
- **Performance**: vsync-locked 16.7 ms at all three pinned viewpoints
  AND W3 combat (worst frame 18.8 ms); ~2.5× pixel headroom; cold start
  ~8 s compute-bound.

## The regression rule

Any change that degrades an item above without Tirtha's explicit
sign-off reverts — the Delight Line applied to a known-good whole.
Compare against the tag: `git diff baseline-2026-08-08 -- <path>`,
and against the audit numbers in performance-audit-2026-07.md Phase E.
